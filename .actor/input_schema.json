{
  "title": "Web Contact Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "type": "array",
      "description": "List of website URLs to extract contact information from",
      "editor": "requestListSources"
    },
    "urls": {
      "title": "URLs (Simple List)",
      "type": "array",
      "description": "Alternative: Simple list of URLs as strings",
      "editor": "stringList",
      "items": {
        "type": "string",
        "pattern": "^https?://"
      }
    },
    "fastMode": {
      "title": "Fast Mode",
      "type": "boolean",
      "description": "Use fast HTML scraping (disable for JavaScript-heavy sites)",
      "default": true
    },
    "maxPages": {
      "title": "Max Pages per Site",
      "type": "integer",
      "description": "Maximum pages to scrape per website (contact, about, team pages)",
      "default": 1,
      "minimum": 1,
      "maximum": 10
    },
    "enableValidation": {
      "title": "Enable Email Validation",
      "type": "boolean",
      "description": "Enable SMTP email validation (slower but more accurate)",
      "default": false
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Proxy settings for requests",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true
      }
    },
    "maxConcurrency": {
      "title": "Max Concurrency",
      "type": "integer",
      "description": "Maximum number of URLs to scrape concurrently",
      "default": 5,
      "minimum": 1,
      "maximum": 20
    },
    "blockKeywords": {
      "title": "Block Keywords",
      "type": "string",
      "description": "Comma-separated keywords to filter out from results (e.g., 'noreply,spam')",
      "editor": "textfield"
    }
  },
  "required": []
}
